export type TableResult = {
  title: string
  author: string,
  publisher: string,
  id: string,
  subtitle: string,
  isbn: string,
  abstract: string,
  price: string,
  numPages: number,
  cover: string,
  userId: number,
};

